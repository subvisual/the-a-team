---
name: ateam-design
description: Use when the A-Team orchestrator invokes the design phase for a feature — produces design.md and a lo-fi prototype in the feature directory. STUB — replace with the real Design-authored skill.
---

# ateam-design (stub)

**No-op placeholder.** The real design skill is authored later by a Designer
against `CONTRACT.md`. This stub only exercises the orchestration wiring.

Given the feature directory `docs/features/<slug>/`, the product directory
`docs/product/`, and `feature.json`:

1. Read the **required floor** — `docs/product/context.md` (including its
   `## Design context` and `## Technical context` sections), every file in
   `docs/product/jtbd/`, and `docs/product/research-plan.md`'s technical
   assumptions + open questions. The research plan is a *required* read, not
   enrichment: jobs cite technical findings rather than restating them, so a
   design that skips it can be specced straight past a constraint the dev
   review already surfaced. Then read `prd.md` and `briefs/` **if present**
   (optional enrichment; the design phase must still run without them).
2. Write `design.md` in the feature directory:
   ```markdown
   # Design (stub) — <slug>

   > Placeholder generated by the ateam-design stub. Replace with the Design skill.

   TODO: IA, user flows, screen/layout direction, visual approach.
   ```
3. Create `docs/features/<slug>/lofi/` with a placeholder `README.md` (throwaway
   visual reference; the real skill produces a lo-fi prototype here).
4. If the orchestrator passed revision notes, append them under a `## Revision notes` heading.
5. Read the current `feature.json`, set **only** `phases.design.status = "complete"`,
   preserve every other field (including `attempts`), write it back. Save.

Do not touch any other manifest field or artifact. Be idempotent — overwrite
`design.md` cleanly if re-invoked, never append duplicates.
